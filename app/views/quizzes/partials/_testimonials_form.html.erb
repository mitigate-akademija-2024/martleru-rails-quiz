<%= form_with(model: [@quiz, @testimonial], local: true) do |form| %>
  <div class="mb-2">
    <%= form.label :testimonial_text, "We Value Your Opinion – Leave a Comment", class: "block text-sm font-medium text-gray-700 mb-1" %>
    <%= form.text_area :testimonial_text, placeholder: "Tell us what your quiz is about...", class: "w-full border border-gray-300 rounded-md shadow-sm focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm px-3 py-2 min-h-[150px] max-h-[250px]" %>
  </div>

  <div class="flex justify-end items-center">
    <%= form.submit "Save", class: get_btn_class %>
  </div>
<% end %>
