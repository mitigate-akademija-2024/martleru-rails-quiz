<section class="px-6 py-8">
  <div class="flex flex-col gap-10">
    <div class="flex flex-col lg:flex-row gap-8">
      <div class="w-full">
        <%= render "quizzes", quizzes: @quizzes %>
      </div>
      <div class="w-full">
        <%= render "results", results: @results %>
      </div>
    </div>
    <div class="w-full">
        <%= render "testimonials", testimonials: @testimonials %>
    </div>
  </div>
</section>
